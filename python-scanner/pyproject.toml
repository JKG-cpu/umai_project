[project]
name = "scanner"
version = "0.1.0"
description = "Python barcode scanner — decode barcodes from images and look up product info via Open Food Facts"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.2.0",
    "pyzbar>=0.1.9",
    "requests>=2.34.2",
]
